import 'package:flutter/material.dart';

class AppColors {
  // Primary background color used on the login scaffold
  static const Color primary = Color(0xFF8B5A3C);

  // Secondary color used for input field backgrounds
  static const Color secondary = Color(0xFFB89178);

  // AppBar color (slightly darker than primary for contrast)
  static const Color appBar = Color(0xFF6D412A);

  // Bottom navigation background and item colors
  static const Color bottomNavBackground = Color(0xFF7A4F36);
  static const Color bottomNavSelected = Color(0xFFFFC107);
  static const Color bottomNavUnselected = Colors.white70;

  // Accent whites
  static const Color white = Colors.white;
  static const Color white70 = Colors.white70;
}
